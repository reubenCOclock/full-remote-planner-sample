    
<div class="container-fluid">
    <nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top">
        <a class="navbar-brand" href="{{ path('main')}}"><img src="{{ asset('img/logo-vbl.png')}}"></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor03">
            <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link btn btn-light btn-lg border border-white rounded-pill" role="button" href="{{ path('userpage' ,{'slug': app.user.slug}) }}">Mon espace</a>
            </li>
            {% if app.user.role.roleTitle == "ROLE_CONSULTANT" %}
            <li class="nav-item">
                <a class="nav-link btn btn-light btn-lg border border-white rounded-pill" role="button" href="{{ path('trombinoscope_user', {'slug': app.user.slug})}}">Team VBL</a>
            </li>
            {% elseif app.user.role.roletitle == "ROLE_RH" %}
            <li class="nav-item">
            <a class="nav-link btn btn-light btn-lg border border-white rounded-pill" role="button" href="{{ path('trombinoscope_rh', {'slug': app.user.slug})}}">Team VBL</a>
            </li>
            {% elseif app.user.role.roletitle == "ROLE_CLIENT"%}
            <li class="nav-item">
            <a class="nav-link btn btn-light btn-lg border border-white rounded-pill" role="button" href="{{ path('trombinoscope_client', {'slug': app.user.slug})}}">Team VBL</a>
            </li>
            {% else %}
            <li class="nav-item">
            <a class="nav-link btn btn-light btn-lg border border-white rounded-pill" role="button" href="{{ path('trombinoscope_admin', {'slug': app.user.slug})}}">Team VBL</a>
            </li>
             {% endif %}
            {# {% if app.user.role.roletitle == "ROLE_ADMIN"%}
            <li class="nav-item">
                <a class="nav-link btn btn-light btn-lg border border-white rounded-pill" role="button" href="{{ path('client_portfolio')}}">Clients VBL</a>
            </li>
            {% endif %} #}
            </ul>
            
            <ul class="navbar-nav ml-auto">
                <li class="nav-item dropdown show">
                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="dropdownMenuLink" aria-haspopup="true" aria-expanded="false">
                    <img src="{{asset('images/avatar/' ~ app.user.imageName)}}" alt="Avatar de {{app.user.imageName}}" class="rounded-pill avatar-mini"> {{app.user.firstName}}
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuLink">
                {% if app.user.role.roleTitle == "ROLE_CONSULTANT" %}
                    <a href="{{ path('account_profile_user',{'slug':app.user.slug})}}" class="dropdown-item">Mon profil</a>
                    <a href="{{ path('account_profile_update_user',{'slug':app.user.slug})}}" class="dropdown-item">Modifier mon profil</a>
                    <a href="{{ path('account_password_user',{'slug':app.user.slug})}}" class="dropdown-item">Modifier mon mot de passe</a>
                    <a href="{{path('uploadInitialDocuments',{'slug':app.user.slug})}}" class="dropdown-item"> Charger mes documents d'initation </a> 

                {% elseif app.user.role.roletitle == "ROLE_RH" %}
                    <a href="{{ path('account_profile_rh',{'slug':app.user.slug})}}" class="dropdown-item">Mon profil</a>
                    <a href="{{ path('account_profile_update_rh',{'slug':app.user.slug})}}" class="dropdown-item">Modifier mon profil</a>
                    <a href="{{ path('account_password_rh',{'slug':app.user.slug})}}" class="dropdown-item">Modifier mon mot de passe</a>
                {% elseif app.user.role.roletitle == "ROLE_CLIENT"%}
                    <a href="{{ path('account_profile_client',{'slug':app.user.slug})}}" class="dropdown-item">Mon profil</a>
                    <a href="{{ path('account_profile_update_client',{'slug':app.user.slug})}}" class="dropdown-item">Modifier mon profil</a>
                    <a href="{{ path('account_password_client',{'slug':app.user.slug})}}" class="dropdown-item">Modifier mon mot de passe</a>
                {% else %}
                    <a href="{{ path('account_profile_admin',{'slug':app.user.slug})}}" class="dropdown-item">Mon profil</a>
                    <a href="{{ path('account_profile_update_admin',{'slug':app.user.slug})}}" class="dropdown-item">Modifier mon profil</a>
                    <a href="{{ path('account_password_admin',{'slug':app.user.slug})}}" class="dropdown-item">Modifier mon mot de passe</a>
                {% endif %}
                        <div class="dropdown-divider"></div>
                    <a href="{{ path('app_logout')}}" class="dropdown-item">Déconnexion</a>   
                </div>
                </li>  
            </ul>
        </div>
    </nav>
</div>
   <div class="container position-relative mt-3">
            {% for label, messages in app.flashes %}

                <div class="d-inline">
                    <div class="text-center alert alert-{{ label }} rounded-pill py-2">
                        {% for message in messages %}
                            <p>{{message | raw}}</p>
                        {% endfor %}
                    </div>
                </div>
            {% endfor %}
    <div class="jumbotron bg-white">


    

